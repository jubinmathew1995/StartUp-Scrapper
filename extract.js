var list = document.getElementsByTagName('div');
for (var i = 0; i < list.length; i++) {
	if(list[i].className == 'show'){
		console.log(list[i]);
	}
	if(list[i].className == 'founders section'){
		console.log(list[i]);
	}
}
